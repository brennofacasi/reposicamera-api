# Coleção de Câmeras API

## Comandos

```bash
(env)$ pip install -r requirements.txt
```

```bash
(env)$ flask run --host 0.0.0.0 --port 5000
```

```bash
(env)$ flask run --host 0.0.0.0 --port 5000 --reload
```